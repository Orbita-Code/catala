# Handover — Katalonski, 30.07.–02.08.2026.

> Za sledeću sesiju. Pročitati celo pre rada.
> Prethodni handover: `HANDOVER-2026-07-26.md`. Testiranje: `QA-HANDOVER.md`, `TESTING.md`.

---

## 1. Ništa ne visi — sve je zatvoreno

Sesija je zatvorena 02.08.2026. **Nema nedovršenih poslova ni otvorenih pitanja.**

Poslednja stavka koja je visila — automatska kapija na GitHub-u (`.github/workflows/kapija.yml`) —
rešena je: vlasnica je tražila da se skine jer je sajt privatan i koristi ga jedno dete,
pa je provera za timove i javne projekte ovde preterivanje.
Fajl je obrisan i **skinut sa GitHub-a** (commit `35d7e99`, uz njeno odobrenje za taj push).

Provere nisu izgubljene — samo se pokreću komandom:

```bash
npm run predeploy        # pre objavljivanja, lokalno
npm run predeploy:prod   # posle objavljivanja, protiv sajta
```

---

## 2. NOVO PRAVILO — pitati pre SVAKOG puširanja

Vlasnica je 02.08.2026. tražila: **pre svakog `git push` se pita.**
Ne važi „odobreno za celu sesiju". Jedno odobrenje = jedan push.
Upisano i u globalni `~/.claude/CLAUDE.md`.

---

## 3. Šta je urađeno u ovoj sesiji

### Maskote (novo)
- Dva superheroja iz `_ARHIVA-AI-GENERACIJE`: **devojčica dole levo, dečak dole desno**.
  24 slike (12 varijanti po liku) u `public/mascot/`, izvorni PNG u `Ilustracije/maskota-superheroj/`.
- `src/lib/mascot.ts` mapira događaj u igri → koja slika. Niska tačnih raste kroz moći
  (brzina → munja → vatra → kosmos). Greška NIJE kazna: lik izvadi štit.
- **Reakcija na ćutanje:** posle 20 s bez dodira oba lika postanu providni „duh".
  Ne požuruje dete, samo ga podseti da je neko tu. Sklanja se na prvi dodir.
- Zvezda (`src/components/star/`) ostaje u savetima, na početnoj i kao slika nivoa.

### Prvi audit projekta ikada
`AUDIT/2026-07-30-audit.md` — ocena 7/10, pokrivenost 7 od 12 dimenzija (pošteno u naslovu).
Projekat do tada nije imao ni `AUDIT/` ni `TESTING.md`.

### Svi nalazi rešeni — bilo 11, sada 0

| Oznaka | Šta je bilo | Šta je sada |
|---|---|---|
| K1 | početna se rušila kad je upis u `localStorage` zabranjen (privatni prozor) | svih 9 upisa ide kroz `src/lib/storage.ts`, ništa ne puca |
| V1 | kontrast dugmadi 1,96 (koliko se tekst razlikuje od pozadine; treba bar 4,5) | 4,77 — `--secondary` sa `#00cece` na `#008080` |
| V2 | početna skakala 130 px pri učitavanju (CLS 0,167; sme najviše 0,1) | CLS 0,02, skok 0 px |
| S1 | 13 od 24 elementa bez vidljivog fokusa tastature | 0 od 23 |
| S2 | dodirne mete do 19 px (treba bar 44) | 0 premalih |
| S3 | URL nije pamtio zadatak; `F5` vraćao na prvi | `?tasca=4`, „Nazad" ide korak po korak, deljiv link radi |
| S4 | pločica razmaka bila prazno žuto dugme | plava, isprekidan okvir, crtica, `aria-label="espai"` |
| S5 | nijedno bezbednosno zaglavlje | 6 zaglavlja; mikrofon namerno ostaje dozvoljen |
| N1 | rupa u ID-jevima (`les-botigues-13`) | **namerno NIJE dirano** — v. tačku 5 |
| N2 | `CLAUDE.md` tvrdio ~226 zadataka | **212**, prebrojano; tabela ispravljena za 8 od 12 tema |
| N3 | `robots.txt` 404 | dodat, zabranjuje indeksiranje |

### Fontovi
Nunito i Comic Neue **jesu pravi** besplatni fontovi (Google Fonts), nisu krpljeni.
Ali su se učitavali preko `@import` u CSS-u — tri odlaska na mrežu pre nego što se tekst
iscrta. Prebačeno na `next/font/google`: skidaju se pri buildu, stoje na našem serveru.
**Izmereno: 0 zahteva ka Google-u.** CSP zato stegnut.

### Pre-deploy kapija
`e2e/predeploy.mjs`, pokreće se sa `npm run predeploy` (lokalno) i `npm run predeploy:prod`.
**11 provera**, svaka nastala iz stvarnog kvara. Izlazni kod 1 = ne sme se objaviti.
Dokazano da radi: pre popravke V2 je padala baš na toj proveri, posle prolazi.

### QA runner
- Popravljeno: razmak se sada **klikće kao pločica**, ne kuca tasterom — zbog toga se
  runner zaglavljivao i tema `les-botigues` nikad nije bila odigrana do kraja.
- Dodato **vremensko ograničenje po zadatku** (`TASK_TIMEOUT_MS`, podrazumevano 45 s):
  zaglavljen zadatak se zabeleži i prolaz ide dalje.
- **Pokrivenost: 204/212 → 212/212 zadataka**, 12/12 tema, 0 grešaka u konzoli,
  0 pokvarenih slika.

### Sadržaj
- `borrador` → **`esborrador`** (katalonski standard) na 11 mesta + slike + odgovornici.
  U `WORKBOOK-VS-ILLUSTRATIONS.md` red „Reči iz sveske" **namerno ostaje `borrador`** —
  to je zapis šta piše u svesci.
- `la-classe-12`: prompt je tražio članove `les`/`els` kojih u zadatku nema, a prećutao
  `l'` koji se bira. Ispravljen.
- **„barra de pa" je provereno u svesci** (`Svi zadaci/IMG_1002.JPG`, vežba 12) — nije
  izmišljeno. Isto i `pa rodó`, `pa de motlle`, `pastís`, `croissant`, `galeta`.

---

## 4. Stanje projekta

| Šta | Vrednost |
|---|---|
| Zadataka | **212** (prebrojano u fajlovima) |
| Tema | 12 |
| Tipova zadataka | 19 |
| Ilustracija | 512 `.webp` + 24 maskote |
| Otvorenih nalaza iz audita | **0** |
| Kapija | 11/11 prolazi, i lokalno i na produkciji |
| Uživo | `catala.orbitacode.com` (iza lozinke) |

---

## 5. ŠTA SE NE DIRA

- **Rupa u ID-jevima `les-botigues-13`.** Napredak deteta se pamti po ID-u zadatka
  (`completedTasks` u `progress.ts`). Prenumerisanje bi svakom detetu poništilo sve
  urađeno u toj temi. Objašnjenje stoji u samom `les-botigues.ts`.
- **`borrador` u redu „Reči iz sveske"** — to je zapis o svesci, ne o aplikaciji.
- **Mikrofon u `Permissions-Policy`** — zadaci samoprocene govora ga koriste.
- **Modal dnevne nagrade** prekriva početnu dok se ne klikne „Continua". Nije bag.
- **`eslint` javlja 1 grešku** u `src/app/page.tsx:32` (`setState` u efektu).
  Postoji od ranije, provereno `git stash`-om. Nije kvar, nije hitno.

---

## 6. Naučeno na svojoj koži (detaljno u `AUDIT/PROPUSTI.md`)

1. **Heš nije pogled.** Presudio sam da su slike maskote „već iskorišćene" na osnovu
   poređenja heševa, a nijednu nisam otvorio. Vlasnica je bila u pravu — 24 slike su
   stajale neupotrebljene. Folder slika se ocenjuje kontakt listom koji se **pogleda**.
2. **Jedan snimak ekrana nije merenje.** Prijavio sam bag koji ne postoji, jer sam
   snimio stranu u onih 200 ms dok se iscrtava.
3. **Novo merilo se prvo pusti na poznatom slučaju.** Moj regex je hvatao `l'` iz reči
   „l'article" i prijavio tri nepostojeća baga.
4. **Nikad ne dirati korisnički profil bez kopije.** Skripta je pala na rezervni kanal i
   otvorila Playwright profil vlasnice **pravim Chrome-om** — profil od tada pada.
5. **Zaglavljen prolaz nije obavljen posao.** Pravilo o automatskoj zameni zaglavljenog
   agenta upisano u `~/.claude/AUDIT-PROTOKOL.md` (odeljak „SAMOOBNAVLJANJE").

---

## 7. Predlog šta dalje

1. **Reakcija po reči, ne po zadatku.** Sada u zadatku od 21 reči dete dobije reakciju
   maskote samo jednom — na kraju. To je najveća preostala prilika: nagrada treba da
   dođe uz trud, a trud je jedna reč, ne dvadeset jedna.
   Radi se u `TemaContent.tsx`; sada se maskota pokreće samo iz `handleTaskComplete`.
2. **Popraviti Playwright profil** iz tačke 6.4, ako zatreba prijava na ChatGPT za nove
   ilustracije. Profil `ms-playwright-mcp/mcp-chrome-for-testing-b5bd434` pada pri
   pokretanju; kolačići su unutra (37 komada), profil je popravljiv, ali se **radi na
   kopiji**, nikad na originalu.
3. **Pre svakog objavljivanja pustiti `npm run predeploy`**, a posle objavljivanja
   `npm run predeploy:prod`. Ako izlazni kod nije 0, ne objavljuje se.

---

## 8. Ako ti je ovo prva sesija na projektu — pročitaj ovim redom

1. `CLAUDE.md` (projekta) — pravilo #1: **sveska je izvor istine**
2. ovaj handover
3. `AUDIT/NALAZI-OTVORENI.md` — trenutno 0 otvorenih, ali tu ide sve novo
4. `TESTING.md` — šta se proverava
5. `AUDIT/PROPUSTI.md` — zašto su neke greške promašene; vrednije od spiska nalaza
