# QA IZVEŠTAJ — Katalonski (25.07.2026)

> Sesija: velika regeneracija ilustracija (33 Bing/tekst slike → 3D cartoon bez teksta) + bug fix +
> čišćenje foldera. Ovaj izveštaj potvrđuje da izmene NISU ništa pokvarile. Deploy: SW v21, commit `d8ccd33`.

## Rezime — ✅ SVE ČISTO

| Provera | Rezultat |
|---|---|
| Oštećene slike (statički, svih 503 ključa) | ✅ **0** (5 mrtvih neiskorišćenih unosa `persona-a..e`) |
| Oštećene slike (browser, 12 tema + home + stats) | ✅ **0** |
| Console greške (12 tema + home + stats) | ✅ **0** |
| TypeScript `tsc --noEmit` | ✅ **0 grešaka** |
| Nove slike na produkciji (byte-match) | ✅ svih provereno = lokalno |
| Bug: zec u domaćim životinjama | ✅ ispravljeno (`conill`→`vaca`) |

## 1. Statička provera oštećenih slika (najsveobuhvatnija)
`getWordIllustration()` prikazuje `<img>` samo za ključeve iz `wordsWithIllustrations` Set-a
(`src/lib/illustrations.ts`, ne proverava fajl). Zato: **oštećena slika = ključ u Set-u bez `.webp`**.
- Ključeva u Set-u: **503**
- Bez `.webp` fajla: **5** → `persona-a, persona-b, persona-c, persona-d, persona-e`
- **Ali:** tih 5 ima **0 referenci** u `src/data/` — nijedan zadatak ih ne prikazuje → **nisu oštećene slike**,
  samo mrtvi unosi u Set-u. (Opciono očistiti kasnije; bezopasno.)
- **Zaključak:** svaka slika koju bilo koji zadatak može prikazati IMA fajl. 0 realnih oštećenih.

## 2. Browser provera (Playwright, headless, localhost)
Učitano: home, svih 12 tema, stats. Mereno: console `error`/`pageerror` + `<img>` sa `naturalWidth===0`.
```
✅ (home)       err=0 broken=0      ✅ les-botigues err=0 broken=0
✅ la-classe    err=0 broken=0      ✅ el-menjar    err=0 broken=0
✅ l-escola     err=0 broken=0      ✅ els-animals  err=0 broken=0
✅ el-cos       err=0 broken=0      ✅ la-ciutat    err=0 broken=0
✅ la-roba      err=0 broken=0      ✅ els-vehicles err=0 broken=0
✅ la-casa      err=0 broken=0      ✅ els-oficis   err=0 broken=0
✅ la-familia   err=0 broken=0      ✅ stats        err=0 broken=0
UKUPNO: console-greške=0, oštećene-slike=0
```

## 3. Produkcija (catala.orbitacode.com)
Nove `.webp` verifikovane preko `curl` (veličina produkcija == lokalno) za: arracades, anell, socorrista,
carnisseria, estetoscopi, cigonya, verduleria, girafa — sve se poklapaju → deploy uspešan, slike LIVE.

## 4. Šta NIJE pokriveno ovde
- **Task-by-task auto-solver** (`e2e/qa/run.mjs`) je pokušan ali je prespor/resursno težak na ovom
  pokretanju (i pravио bi lažne „stuck" na novim tipovima zadataka els-animals-4b/6c koje solver još
  ne zna). Za sesiju zamene SLIKA, provere #1–#3 su merodavne. Za dublji test logike zadataka pokrenuti
  solver zasebno kad ima vremena i dopuniti solvere za nove zadatke.

## Metod (ponovljivo)
- Statički: izvuci ključeve iz `wordsWithIllustrations` (linije ~394–929 `illustrations.ts`), proveri
  `public/illustrations/<key>.webp`; ukrsti sa `grep -r <key> src/data/` za realnu upotrebu.
- Browser: headless Playwright, `goto` svake teme, skupi console errors + `img.naturalWidth===0`.
